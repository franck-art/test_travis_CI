language: bash

sudo: required

service: docker

before_install: apt-get update -y

install: 
  - apt install epel-release
  - apt install python-pip

script:
  - echo " installation docker"
  - echo ` curl -fsSL https://get.docker.com -o get-docker.sh`
  - echo `sh get-docker.sh`
  - echo 'fin du script'

after_script: 
  - echo "fin"
